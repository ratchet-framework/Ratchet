# Private operational files — never commit these
memory/
workspace-index.json
embeddings.json
facts-*.jsonl
facts-purged.jsonl
facts-rejected.jsonl
extraction-errors.jsonl
memory-log.jsonl
patterns.json
*.env
secrets/
MEMORY.md
trust.json

# Secrets and credentials
.env.*
credentials/

# Personal context — never commit real user data
context.local.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Logs
*.log

# Local/temp screenshots (root only — docs/screenshots is intentional)
/screenshots/
